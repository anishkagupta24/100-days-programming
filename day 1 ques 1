//write a program to input two numbers and calculate their sums
#include <stdio.h>

int main() 
{
    float num1, num2, sum;
    printf("enter the first number");
    scanf("%f", &num1);
    printf("enter the first numebr2");
    scanf("%f", &num2);
    sum=num1+num2;
    printf("the sum is %.2f\n",sum);
    return 0;
}
